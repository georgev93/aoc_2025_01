pub struct Rotations {
    path: String,
}

impl Rotations {
    pub fn new(path: String) -> Self {
        Self {
            path,
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    // #[test]
}
